# LightHouse Wrapper for JobRad.org

## Prerequisites 

- install required packages via `yarn`

## Run tests

- configure the urls to test in `resources/data.json`
- perform `yarn start` in the root directory
- find your reports in the `reports` folder